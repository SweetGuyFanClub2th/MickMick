<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.main.friend.FriendListActivity">

    <ImageView
        android:id="@+id/friend_search_bg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/search_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="100dp"
        android:layout_marginStart="35dp"
        />

    <EditText
        android:id="@+id/FriendSearchId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/friend_search_bg"
        app:layout_constraintStart_toStartOf="@id/friend_search_bg"
        android:layout_marginStart="10dp"
        android:textSize="20sp"
        android:background="@null"
        />

    <ImageButton
        android:id="@+id/FriendSearchBt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/search_button"
        app:layout_constraintTop_toTopOf="@id/friend_search_bg"
        app:layout_constraintEnd_toEndOf="@id/friend_search_bg"
        android:layout_marginTop="13dp"
        android:layout_marginEnd="15dp"
        />
</androidx.constraintlayout.widget.ConstraintLayout>